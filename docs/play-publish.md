# Publish TapBoard APK to GitHub Releases

```bash
python scripts/publish_release.py
```

Builds the signed release APK and replaces `TapBoard.apk` on release `v1.0.0`.

Bluetooth-only app — no Windows companion.
